- if user_signed_in?
	-if current_user.blind_date == "yes"
		- @showFollowing = true
		- @followValue = "Disable blinddating"
		
	-else
		- @showFollowing = false
		- @followValue = "Enable blinddating"
		
	.col.s4
		= react_component 'BlindDate', {followValue: @followValue, showFollowing: @showFollowing, userId: current_user.id}